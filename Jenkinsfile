pipeline {
    agent { label 'slave1' }
    stages {
        stage('checkout') {
            steps {
                sh "git clone https://github.com/sarath8008/project2.git"
            }
        }
    }
}
